# Database Configuration
# Copy this file to .env and update values as needed: cp .env.example .env
#
# For local development with Docker Compose (dev container), use:
# DATABASE_URL="postgresql://taskflow:taskflow@postgres:5432/taskflow"
#
# For local development with a locally running PostgreSQL instance:
# - If using Postgres.app on Mac (no password, uses your Mac username):
# DATABASE_URL="postgresql://localhost:5432/taskflow"
# - If you have a PostgreSQL user with password:
DATABASE_URL="postgresql://taskflow:taskflow@localhost:5432/taskflow"

# Node Environment
NODE_ENV="development"
